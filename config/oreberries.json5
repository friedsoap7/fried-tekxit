{
	"copper": {
		"oreBerryColor": "#FF8833",
		"nuggetColor": "#FF6600",
		// The tooltip for Copper OreBerry
		"tooltip": "Tastes like metal",
		// Whether the berry bush can grow in the light or not
		"growsInLight": false,
		// Max berry drops
		"maxDrop": 3,
		// Min berry drops
		"minDrop": 1,
		// Whether this berry will be generated in the world
		"generate": true,
		// Max ore berry vein size chance
		"veinSize": 8,
		// Number of veins per chunk. Defaults to 6
		"rarity": 6,
		// The lowest height where bushes will generate at. Defaults to 0.
		"minHeight": 0,
		// The bushes will be generated around this maximum height. Defaults to 60.
		"maxHeight": 60
	},
	"tin": {
		"oreBerryColor": "#BB4422",
		"nuggetColor": "#FFBA80",
		// The tooltip for Tin OreBerry
		"tooltip": "Tin Man",
		// Whether the berry bush can grow in the light or not
		"growsInLight": false,
		// Max berry drops
		"maxDrop": 3,
		// Min berry drops
		"minDrop": 1,
		// Whether this berry will be generated in the world
		"generate": true,
		// Max ore berry vein size chance
		"veinSize": 8,
		// Number of veins per chunk. Defaults to 7
		"rarity": 7,
		// The lowest height where bushes will generate at. Defaults to -16.
		"minHeight": -16,
		// The bushes will be generated around this maximum height. Defaults to 40.
		"maxHeight": 40
	},
	"aluminum": {
		"oreBerryColor": "#EEFFFF",
		"nuggetColor": "#EEFFFF",
		// The tooltip for Aluminum OreBerry
		"tooltip": "White Chocolate",
		// Whether the berry bush can grow in the light or not
		"growsInLight": false,
		// Max berry drops
		"maxDrop": 3,
		// Min berry drops
		"minDrop": 1,
		// Whether this berry will be generated in the world
		"generate": true,
		// Max ore berry vein size chance
		"veinSize": 8,
		// Number of veins per chunk. Defaults to 7
		"rarity": 7,
		// The lowest height where bushes will generate at. Defaults to -16.
		"minHeight": -16,
		// The bushes will be generated around this maximum height. Defaults to 60.
		"maxHeight": 60
	},
	"iron": {
		"oreBerryColor": "#FFC7A3",
		// The tooltip for Iron OreBerry
		"tooltip": "Sweet Irony",
		// Whether the berry bush can grow in the light or not
		"growsInLight": false,
		// Max berry drops
		"maxDrop": 3,
		// Min berry drops
		"minDrop": 1,
		// Whether this berry will be generated in the world
		"generate": true,
		// Max ore berry vein size chance
		"veinSize": 9,
		// Number of veins per chunk. Defaults to 10
		"rarity": 10,
		// The lowest height where bushes will generate at. Defaults to -16.
		"minHeight": -16,
		// The bushes will be generated around this maximum height. Defaults to 64.
		"maxHeight": 64
	},
	"gold": {
		"oreBerryColor": "#FFCC33",
		// The tooltip for Gold OreBerry
		"tooltip": "Pure Luster",
		// Whether the berry bush can grow in the light or not
		"growsInLight": false,
		// Max berry drops
		"maxDrop": 3,
		// Min berry drops
		"minDrop": 1,
		// Whether this berry will be generated in the world
		"generate": true,
		// Max ore berry vein size chance
		"veinSize": 6,
		// Number of veins per chunk. Defaults to 6
		"rarity": 6,
		// The lowest height where bushes will generate at. Defaults to -16.
		"minHeight": -16,
		// The bushes will be generated around this maximum height. Defaults to 32.
		"maxHeight": 32
	},
	"essence": {
		"tooltip": "Tastes like Creeper",
		// Whether the berry bush can grow in the light or not
		"growsInLight": true,
		// Max berry drops
		"maxDrop": 3,
		// Min berry drops
		"minDrop": 1,
		// Whether this berry will be generated in the world
		"generate": true,
		// Max ore berry vein size chance
		"veinSize": 7,
		// Number of veins per chunk. Defaults to 6
		"rarity": 6,
		// The lowest height where bushes will generate at. Defaults to -16.
		"minHeight": -16,
		// The bushes will be generated around this maximum height. Defaults to 32.
		"maxHeight": 32
	}
}